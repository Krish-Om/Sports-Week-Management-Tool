# 🏗️ Tailscale Deployment Architecture

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        Tailscale Network                         │
│                    (Encrypted VPN Mesh)                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Student    │  │   Faculty    │  │   Admin      │          │
│  │   Device     │  │   Device     │  │   Device     │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                 │                  │                   │
│         └─────────────────┴──────────────────┘                   │
│                           ▼                                      │
│         ┌─────────────────────────────────────────┐             │
│         │      http://<device>.tail-scale.ts.net  │             │
│         └─────────────────┬───────────────────────┘             │
└───────────────────────────┼─────────────────────────────────────┘
                            │
                            ▼
     ┌──────────────────────────────────────────────────────────┐
     │              Your Ubuntu Home Server                      │
     │              (Dell Laptop)                                │
     │                                                            │
     │  ┌────────────────────────────────────────────────────┐  │
     │  │              Tailscale Daemon                       │  │
     │  │  • Manages encrypted connections                   │  │
     │  │  • Provides stable hostname                        │  │
     │  │  • Handles NAT traversal                          │  │
     │  └────────────────────────────────────────────────────┘  │
     │                           │                                │
     │                           ▼                                │
     │  ┌────────────────────────────────────────────────────┐  │
     │  │         Docker Compose Stack                       │  │
     │  │                                                     │  │
     │  │  ┌─────────────────────────────────────────────┐  │  │
     │  │  │  Nginx (Port 80)                            │  │  │
     │  │  │  • Reverse proxy                            │  │  │
     │  │  │  • Routes traffic                           │  │  │
     │  │  │  • WebSocket support                        │  │  │
     │  │  └──────┬──────────────────────┬───────────────┘  │  │
     │  │         │                      │                   │  │
     │  │         ▼                      ▼                   │  │
     │  │  ┌──────────────┐      ┌──────────────┐          │  │
     │  │  │   Frontend   │      │   Backend    │          │  │
     │  │  │  (Port 5173) │      │  (Port 3001) │          │  │
     │  │  │              │      │              │          │  │
     │  │  │  React App   │◄────►│  Express API │          │  │
     │  │  │  + Vite      │      │  + Socket.io │          │  │
     │  │  │  + Tailwind  │      │  + Drizzle   │          │  │
     │  │  └──────────────┘      └──────┬───────┘          │  │
     │  │                                │                   │  │
     │  │                                ▼                   │  │
     │  │                        ┌──────────────┐           │  │
     │  │                        │  PostgreSQL  │           │  │
     │  │                        │  (Port 5432) │           │  │
     │  │                        │              │           │  │
     │  │                        │   Database   │           │  │
     │  │                        └──────────────┘           │  │
     │  │                                                     │  │
     │  └─────────────────────────────────────────────────────┘  │
     │                                                            │
     └────────────────────────────────────────────────────────────┘
```

## Data Flow

### User Request Flow

```
User Device → Tailscale → Server → Nginx → Frontend/Backend → Database
    ↑                                                               │
    └───────────────────Response Flow ←─────────────────────────────┘
```

### Real-Time Updates (Socket.io)

```
Admin Updates Score
        ↓
    Backend API
        ↓
    Socket.io Emit
        ↓
    ├─→ Manager Devices (WebSocket)
    ├─→ Student Devices (WebSocket)
    └─→ Public Dashboard (WebSocket)
```

## Network Topology

### Internal (Docker Network)

```
┌────────────────────────────────────────────┐
│    Docker Network: sports-network          │
│                                             │
│  nginx:80 ──┐                              │
│             ├──→ frontend:5173              │
│             └──→ backend:3001               │
│                      │                      │
│                      └──→ postgres:5432     │
│                                             │
└────────────────────────────────────────────┘
```

### External Access

```
Internet Users (Optional - with Funnel)
        │
        ▼
┌───────────────────┐
│  Tailscale Funnel │
│  HTTPS Endpoint   │
└────────┬──────────┘
         │
         ▼
Tailscale Network Users (Primary)
         │
         ▼
┌────────────────────┐
│  Tailscale VPN     │
│  100.x.x.x         │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│  Your Home Server  │
│  Nginx:80          │
└────────────────────┘
```

## File Structure

```
Sports-Week-Management-Tool/
│
├── docker-compose.homeserver.yml    # Docker services configuration
├── nginx.conf                       # Nginx reverse proxy config
├── .env                            # Environment variables (created by script)
│
├── deploy-tailscale.sh             # 🚀 Main deployment script
│
├── TAILSCALE_DEPLOYMENT.md         # Complete deployment guide
├── QUICK_START_TAILSCALE.md        # Quick reference
├── DEPLOYMENT_COMPARISON.md        # Compare deployment options
├── DEPLOYMENT_CHECKLIST.md         # Step-by-step checklist
│
├── backend/
│   ├── Dockerfile
│   ├── src/
│   └── ...
│
└── frontend/
    ├── Dockerfile
    ├── src/
    └── ...
```

## Deployment Process Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    Deployment Process                            │
└─────────────────────────────────────────────────────────────────┘

1. Prerequisites Check
   ├── Docker installed?
   ├── Docker Compose installed?
   └── Tailscale installed?
           │
           ▼
2. Tailscale Setup
   ├── Install Tailscale
   ├── Authenticate user
   └── Get device hostname
           │
           ▼
3. Configuration
   ├── Generate secure passwords
   ├── Create .env file
   └── Set up Tailscale URLs
           │
           ▼
4. SSL Certificates
   ├── Check for Tailscale certs
   ├── Copy if available
   └── Generate self-signed if not
           │
           ▼
5. Docker Images
   ├── Pull from GitHub Container Registry
   └── Use ghcr.io/krish-om/* images
           │
           ▼
6. Docker Compose
   ├── Start all services
   ├── postgres (database)
   ├── backend (API + Socket.io)
   ├── frontend (React app)
   └── nginx (reverse proxy)
           │
           ▼
7. Health Checks
   ├── Wait for services
   ├── Test endpoints
   └── Verify connections
           │
           ▼
8. Success! 🎉
   └── App accessible via Tailscale URL
```

## Security Layers

```
┌─────────────────────────────────────────────────┐
│  Layer 1: Tailscale Encryption                  │
│  • WireGuard protocol                           │
│  • End-to-end encryption                        │
│  • Device authentication                        │
└────────────────┬────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────┐
│  Layer 2: Firewall (UFW)                        │
│  • Block all incoming except SSH                │
│  • Allow Tailscale subnet                       │
│  • Deny everything else                         │
└────────────────┬────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────┐
│  Layer 3: Docker Network Isolation              │
│  • Internal network only                        │
│  • No direct port exposure                      │
│  • Container-to-container communication         │
└────────────────┬────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────┐
│  Layer 4: Application Authentication            │
│  • JWT tokens                                   │
│  • Role-based access (Admin/Manager)           │
│  • Secure password hashing                      │
└─────────────────────────────────────────────────┘
```

## Traffic Patterns

### Admin/Manager Access

```
Admin Device
    │
    ├─ Login: POST /api/auth/login
    │   └─→ Receive JWT token
    │
    ├─ Manage Games: GET/POST/PUT /api/games
    │
    ├─ Update Scores: PUT /api/matches/:id/score
    │   └─→ Socket.io broadcasts → All connected clients
    │
    └─ View Reports: GET /api/points/summary
```

### Public/Student Access

```
Student Device
    │
    ├─ View Dashboard: GET /
    │
    ├─ View Fixtures: GET /fixtures
    │
    ├─ View Leaderboard: GET /leaderboard
    │
    └─ WebSocket Connection → Receive live updates
        • score updates
        • match status changes
        • leaderboard changes
```

## Scaling Options

### Current Setup (Single Server)

```
All services on one machine
• Good for: 100-500 concurrent users
• Cost: $0 (home server)
• Maintenance: Manual
```

### Future Scaling Path

```
┌──────────────────────────────────────┐
│  Option 1: Vertical Scaling          │
│  • Upgrade server hardware           │
│  • More RAM, faster CPU              │
│  • Good for: 500-1000 users          │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│  Option 2: Horizontal Scaling        │
│  • Multiple backend instances        │
│  • Load balancer (nginx)             │
│  • Shared database                   │
│  • Good for: 1000+ users             │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│  Option 3: Cloud Migration           │
│  • AWS, GCP, DigitalOcean            │
│  • Auto-scaling                      │
│  • CDN for static assets             │
│  • Good for: Unlimited scale         │
└──────────────────────────────────────┘
```

## Monitoring Points

```
┌─────────────────────────────────────────┐
│         Monitoring Dashboard            │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  System Metrics                 │   │
│  │  • CPU usage                    │   │
│  │  • Memory usage                 │   │
│  │  • Disk space                   │   │
│  │  • Network traffic              │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  Docker Metrics                 │   │
│  │  • Container health             │   │
│  │  • Restart count                │   │
│  │  • Resource usage per container │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  Application Metrics            │   │
│  │  • API response times           │   │
│  │  • WebSocket connections        │   │
│  │  • Database queries             │   │
│  │  • Error rates                  │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  Tailscale Metrics              │   │
│  │  • Connected devices            │   │
│  │  • Bandwidth usage              │   │
│  │  • Connection status            │   │
│  └─────────────────────────────────┘   │
│                                         │
└─────────────────────────────────────────┘
```

## Backup Strategy

```
┌────────────────────────────────────────┐
│           Backup Schedule              │
├────────────────────────────────────────┤
│                                        │
│  Daily (Automated)                     │
│  └─→ Database dump                     │
│      • Full PostgreSQL backup          │
│      • Stored locally                  │
│                                        │
│  Weekly (Automated)                    │
│  └─→ Configuration backup              │
│      • .env file                       │
│      • docker-compose.yml              │
│      • nginx.conf                      │
│                                        │
│  Monthly (Manual)                      │
│  └─→ Off-site backup                   │
│      • Copy to cloud storage           │
│      • Verify restore process          │
│                                        │
└────────────────────────────────────────┘
```

---

## Quick Reference URLs

After deployment, bookmark these:

- **App**: `http://<your-device>.tail-scale.ts.net`
- **Health Check**: `http://<your-device>.tail-scale.ts.net/health`
- **API Health**: `http://<your-device>.tail-scale.ts.net/api/health`
- **Tailscale Admin**: https://login.tailscale.com/admin

## Useful Commands

```bash
# Check everything
./deploy-tailscale.sh

# View logs
docker compose -f docker-compose.homeserver.yml logs -f

# Restart all
docker compose -f docker-compose.homeserver.yml restart

# Stop all
docker compose -f docker-compose.homeserver.yml down

# Check Tailscale
tailscale status

# Enable public access
tailscale funnel 80
```

---

This architecture provides a secure, scalable, and easy-to-maintain deployment for your Sports Week Management Tool!
